version: "3.8"

services:
  iframe-dashboard:
    build: .
    container_name: iframe-dashboard
    ports:
      - "8000:80"
    volumes:
      - ./services.yaml:/app/services.yaml
    restart: unless-stopped
